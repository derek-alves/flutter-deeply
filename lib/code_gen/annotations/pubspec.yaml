name: annotations
description: "Custom annotations for code generation."
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.7.0 <4.0.0"

dev_dependencies:
  lints: ^3.0.0 # Optional for linting
